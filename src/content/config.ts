// Portal content is no longer built into this (public) site. As of Phase 2b it
// is fetched at request time from the private source repo, scoped to the
// signed-in client (see src/lib/github.ts). No build-time collections.
export const collections = {};
